# Agent Instructions

- Always use the Python environment under `.venv` for Python commands, scripts, tests, dependency inspection, and package installation.
- Prefer invoking Python as `./.venv/bin/python` and tools as `./.venv/bin/<tool>` from the repository root.
- If the virtual environment is missing or unusable, stop and ask before using any other Python environment.
